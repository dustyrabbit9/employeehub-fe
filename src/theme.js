import { createTheme } from "@mui/material";
import { color } from "@mui/system";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#146C94",
      light: "#19A7CE",
    },
    secondary: {
      main: "#F6F1F1",
      light: "#FFFFFF",
    },
    otherColor: {
      main: "#146C94",
    },
  },
});
